# E2E Tests — Implementation Plan

## What exists

- E2E test files in tests/e2e/ directory
- Component import validation tests
- Browser-based testing capability (Playwright MCP available)

## What's been done

- E2E test structure established for feature flags, A/B testing,
  search UI, media, blog, comments, newsletter, moderation
- Component import validation ensures Svelte components compile
- Admin and app route test patterns established
